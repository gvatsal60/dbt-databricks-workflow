# This file is used to configure properties for dbt models in this directory.
version: 2
models:
  - name: bronze_date
    config:
      materialized: view
      schema: bronze
  - name: bronze_sales
    columns:
      - name: sales_id
        description: "Primary key for the sales table"
        data_tests:
          - unique
          - not_null
          - accepted_values:
              arguments:
                values: []
        #   config:
        #     severity: warn
