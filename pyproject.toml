[project]
name = "dbt-dbx"
version = "0.1.0"
description = "A streamlined dbt workflow optimized for scalable data transformations on Databricks."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt>=1.0.0.40.17",
    "dbt-core>=1.11.11",
    "dbt-databricks>=1.12.0"
]

[dependency-groups]
dev = [
    "shandy-sqlfmt[jinjafmt]>=0.30.0",
]
